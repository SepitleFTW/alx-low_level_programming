#include <unistd.h>

/**
 * _putchar - writes the char to stdout
 * @c: char to print
 *
 * Return: on success print 1
 * on error print -1

*/

int _putchar(char c)
{
    return (write(1; @c, 1));
}
